# Progress Log — Geocoding ML Project

Update this file as work happens. Append new entries; don't rewrite old ones unless correcting them.

## Roadmap steps completed
- **Step 1 (dumb baseline) — CORRECTION (2026-09-13): never actually saved, treat as not done.** The pipeline described below was reported as built on 2026-09-05, but `step1-baseline/` does not exist on disk and never appears in git history — it was lost (likely built in a session whose work wasn't committed). Resetting roadmap status to ⬜. Original (now-stale) description, kept for reference: code was reported to live in `step1-baseline/` (`generate_dataset.py`, `features.py`, `train.py`), features being raw/number/street-name/street-type/zip similarity via jellyfish Jaro-Winkler fed into sklearn LogisticRegression, run against a synthetic placeholder dataset split 70/15/15 by address id, reporting test F1 = 0.9945 (flagged even then as inflated by easy synthetic negatives). None of this should be cited or relied on.
- **Step 1b (stronger feature-engineered baseline) — 🔶 in progress (as of 2026-09-13).** Actively being worked on now, in parallel with Step 2 dataset setup.
- **Step 2 (real dataset) — 🔶 in progress (as of 2026-09-13), not previously logged.** Downloaded a real Stamford, CT address dataset (shapefile) into `Data/us-ct-city_of_stamford/`. Wrote `Data/Data_Helpers/convert_file_to_csv.py` (uses `pyshp`) to convert it to `output.csv` — 28,294 rows with columns StreetName, Address, ZipCode, X_COORD, Y_COORD. Started exploring it in `Data/Data_Helpers/stamford_analysis.ipynb` (currently just loads and previews the CSV; has uncommitted changes). Not yet done: address-component parsing, positive/hard-negative pair generation, or the location-based train/val/test split.
- **Step 2 (real dataset) continued — 2026-09-14, address-component exploration in `stamford_analysis.ipynb`.** Built suffix/prefix frequency analysis on `StreetName` (last/first token via `.str.split()` + `.str[-1]`/`.str[0]`, tallied with `value_counts()`, mapped back per-row with `.map()`). Found directional qualifiers (`N`/`S`/`E`/`W` only — no `NE`/`NW`/`SE`/`SW` combos appear in this dataset) show up as both a leading prefix (`W HAVILAND LN`) and a trailing suffix (`DAVENPORT FARM LN W`), which were initially polluting the street-type suffix counts (e.g. `WHITE BIRCH RD S` was reading suffix as `S` instead of `RD`). Verified via a combined `.isin()` check (not equality — see debugging log) that no single row has a directional at *both* the prefix and suffix position, so no precedence/tie-break rule is needed. Built `get_directional`/`drop_directional` functions (via `.apply()`) that extract the directional into its own `Directional` column and strip it out of `StreetName`; spot-checked against the `SPRING HILL LN N`/`SPRING HILL LN E` rows and it works correctly. Not yet consolidated into a single-pass `.apply()` (currently two separate passes, each re-deriving the split token list) — noted as a later optimization, not urgent. `EXT` (e.g. `DUNN AVE EXT`) was noticed as a similar trailing-qualifier case but not yet investigated — see open questions. Address-component parsing overall still not complete: no house-number/ZIP parsing decisions made yet, and positive/hard-negative pair generation and the train/val/test split still haven't started.

- **Step 2 (real dataset) continued — 2026-09-15, address-component parsing and corruption function in `stamford_analysis.ipynb`.** Street-name-side component parsing is now essentially complete: added a stable `id` column (sequential int assigned immediately after `pd.read_csv`, via `.insert(0, "id", ...)`, so it survives later sorting/reindexing); built `EXT`-qualifier detection (`is_extensional` column) + stripping, mirroring the existing `Directional` extraction pattern, after confirming via full-dataset check that `EXT` only ever appears at the trailing position (2 rows total, both `DUNN AVE EXT`, never a prefix or mid-string); extracted the real street-type suffix into `street_extension` (e.g. `RD`, `AVE`, `ST`) after confirming 100% of rows' remaining last token matches a closed, dataset-derived 24-value `known_suffixes` list (0 unmatched). Also fixed a real data bug: `ZipCode` was silently losing its leading zero (`06903` → `6903`) — root cause was `pd.read_csv`'s automatic dtype inference, not the shapefile (which stores it correctly as a char field) or `convert_file_to_csv.py` (whose `output.csv` is also correct); fixed via `dtype={"ZipCode": str}` at read time. Confirmed house-number (`Address`) needs no parsing — every row is already a clean integer, no units/ranges/alpha suffixes.
- **Step 2 continued — positive-pair corruption (abbreviation substitution) built, 2026-09-15.** Sourced the official USPS Publication 28 Appendix C1 "Street Suffix Abbreviations" table (503 rows) and Appendix I4 "Directionals" table directly from pe.usps.com, saved as `Data/usps_pub28_street_suffix_abbreviations.csv`. Built `sfx_abrev_lookup` (grouped/deduped by standard abbreviation, self-value excluded from each variant list so a swap always changes something) and a small `directional_lookup` (`N/S/E/W` → full word). Built `_corrupt_row`/`corrupt_rows` in the notebook (not yet promoted to `corrupt_data.py` — deliberately deferred; see design decision below) implementing abbreviation-substitution corruption: independent 70% per-row trigger for `street_extension` and `directional` fields, sampling a random non-self variant when eligible. Confirmed 27,222 of 28,294 rows are actually eligible for suffix corruption; the rest (`QUAY`, `DOCK` — absent from the USPS table entirely — and `PL`, which only has itself as a commonly-used variant) always fall back unchanged. Corrupted output retains an `original_id` column (renamed from `id`) referencing the source row, so a labeled positive-pairs table can be built later via a join on that id.
- **Design decisions locked in for the corruption architecture (2026-09-15):** corrupted rows are represented as structured field-sets, not reconstructed address strings — deliberately grounded in Comber & Arribas-Bel's own pipeline (CRF parser → structured comparison vectors → classifier), since a real address-matching model ingests already-parsed components rather than raw strings, so directional prefix/suffix position never needs to be preserved. Corruption "intensity" is modeled as independent per-method probability gating (not a single global corruption-level scale) — each method has its own trigger probability, and multiple methods can co-fire on the same row. Character-level replacement corruption (per-character probability of substitution/deletion) was discussed but deferred to a future session — open design questions logged below.

## Papers reviewed
| Paper | Date reviewed | One-line takeaway |
|---|---|---|
| Comber & Arribas-Bel (2019), "Machine learning innovations in address matching: A practical comparison of word2vec and CRFs" (Transactions in GIS 23(2):334-348) | 2026-09-13 | This is Step 1's actual paper -- CRF-based address parsing (Libpostal, pretrained on OSM) feeding Jaro-Winkler comparison vectors into logistic/RF/XGBoost (best: XGBoost, precision 0.955, recall 0.902), vs. swapping in word2vec cosine-similarity comparison vectors (best: XGBoost, precision 0.950, recall 0.870 -- competitive but doesn't win). Negative pairs generated synthetically via FEBRL character-level corruption of real matched pairs, not random pairing. Notably, their "baseline" is already parser+comparison-vector+classifier, not a single raw-string score -- worth knowing before implementing our own dumb baseline. |
| Lee, Claridades & Lee (2020), "Improving a Street-Based Geocoding Algorithm Using Machine Learning Techniques" (Applied Sciences 10(16):5628) | 2026-09-05 | Feature-engineered classical baseline (17 string-similarity metrics -> XGBoost) hits 96%+ accuracy on Korean street addresses without any embeddings/neural net; added to roadmap as Step 1b, a stronger classical floor to beat before Steps 3-5. |

## Concepts already explained / understood
- **pandas `.str` accessor vs. indexing a Series directly** — `series.str.split()` returns a Series of lists; `[-1]`/`[0]` on the *Series itself* does label-based lookup against its index (raises `KeyError` on a RangeIndex, since there's no label `-1`), not "get the last element of each row's list." Use `.str[-1]`/`.str[0]` to index into each row's list elementwise. Covered 2026-09-14.
- **`Series.map()` as a lookup/join against another Series or dict** — `value_counts()` returns a Series shaped exactly like a lookup table (index = unique value, value = count); `.map()` uses that to replace each row's value with its corresponding count (or any other keyed lookup). Covered 2026-09-14.
- **Boolean mask combination: `&`/`|` vs. Python's `and`/`or`** — `and`/`or` try to evaluate the truthiness of an entire Series (ambiguous for length >1, raises `ValueError`), while `&`/`|` are pandas-overridden operators that combine two boolean Series elementwise. Operator precedence also means each condition needs its own parentheses when combined this way. Covered 2026-09-14 (surfaced through a real `ValueError: truth value of a Series is ambiguous` debugging session).
- **`df.copy()` deep-copies by default** — no need for Python's `copy.deepcopy()`; `.copy()` already does what's needed to avoid two names pointing at the same underlying DataFrame. Covered 2026-09-14.
- **`.apply()` mechanics** — runs a function once per row/value; returning a single value produces one new column, returning a `pd.Series` with named fields lets pandas expand it into multiple new columns at once (`df[["a","b"]] = series.apply(fn)`). Covered 2026-09-14.
- **`sort_values()` vs. `sort_index()`**, and that `value_counts()` is already sorted descending by count by default. Covered 2026-09-14.
- **`DataFrame.insert()` is in-place/mutating and returns `None`** — same category as `list.sort()`. Caught as a real bug: `csv["id"] = csv.insert(0, "id", [...])` first correctly inserts `id` via the side effect, then immediately overwrites it with `None` (the method's return value). Fix: just call `.insert()` on its own, no outer assignment. Covered 2026-09-15.
- **Notebook cell statefulness — output can silently go stale relative to source.** If a cell mutates a persistent variable in place (or partially, across a multi-line cell) and gets edited/rerun, the *displayed* output can reflect a stale prior run rather than the current code — hit repeatedly this session (an `EXT` detection false-negative from rerunning a cell that had already stripped `EXT` the first time; a `groupby().to_dict()` cell displaying a stale raw DataFrame instead of the actual dict; `corrupt_rows` appearing to produce unchanged output twice in a row after real fixes). Diagnosed each time by independently reproducing the *current* saved source outside the notebook and comparing to the notebook's displayed output. Covered/reinforced multiple times, 2026-09-15.
- **Python global-namespace shadowing of builtins.** `for key, list in some_dict.items():` rebinds the name `list` in the whole notebook session, not just the loop body — a later, unrelated call to `.apply(list)` then breaks with `TypeError: unhashable type: 'list'`, because `list` no longer refers to the builtin constructor. Fix required both renaming the loop variable *and* restarting the kernel, since the corrupted global binding persists in memory until restart. Covered 2026-09-15.
- **`list.find()` doesn't exist; `list.index()` raises `ValueError` if not found (unlike `str.find()`'s `-1`); and `if index:` is a truthiness check, not a found/not-found check** — silently wrong when a match is found at position 0 (`if 0:` is `False`). Covered 2026-09-15.
- **Row-as-Series round-tripping:** a single row pulled from a DataFrame (`df.loc[i]`, or the second element of an `iterrows()` tuple) collapses to one shared `dtype` (usually `object`), but `pd.DataFrame(list_of_row_series_or_dicts)` correctly re-infers proper per-column dtypes on reconstruction — verified this empirically rather than assuming. The real risk going Series→DataFrame isn't dtype loss; it's implicit, silently-permissive index alignment (a typo'd/missing key doesn't error, it just produces `NaN` or a stray column) and orientation ambiguity (`.to_frame().T` needed to turn a Series into a one-row rather than one-column DataFrame). Covered 2026-09-15.
- **`df.iterrows()` yields `(index, Series)` tuples**, not just the Series — `for row in df.iterrows()` gives you the whole tuple; forgetting to unpack (`for _, row in df.iterrows()`) causes e.g. `row.copy()` to fail with `AttributeError: 'tuple' object has no attribute 'copy'` since tuples have no `.copy()`. Covered 2026-09-15.
- **`DataFrame.groupby()` as the idiomatic "for each unique value, act on all its rows" iterator** — directly iterable as `(key, group_df)` pairs, and collapses to a one-liner via `.groupby(col)[other_col].apply(list).to_dict()` when you just want the finished lookup dict rather than an explicit loop. Covered 2026-09-15.
- **`DataFrame.rename()` (like most pandas ops) returns a new object rather than mutating in place** — needs reassignment. Also caught a real bug from reusing one variable name for two different DataFrames in the same function: `new_df = pd.DataFrame(corrupted_rows); new_df = df.rename(...)` — the second line overwrote the actual corrupted result with a renamed copy of the original, uncorrupted input. Covered 2026-09-15.

## Background already established
- Software engineer, ex-Ping Data Intelligence (Ping.Maps, tile server pipelines, SQL optimization, SOV text representation for ML pretraining)
- Strong GPT-2-level transformer/NN background coming in
- Working on address matching as the chosen narrow task (not parsing, not reverse geocoding)
